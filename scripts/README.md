# RideLink Helper Scripts

This directory contains convenience scripts for building and executing microservices locally.

- `build-all.ps1`: Builds all 4 microservices using Maven.
- `test-all.ps1`: Runs unit and integration tests across all services.
