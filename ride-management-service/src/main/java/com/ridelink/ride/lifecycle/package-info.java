/**
 * State machine and lifecycle transition contracts for Ride status changes
 * (REQUESTED -> ASSIGNED -> ACCEPTED -> IN_PROGRESS -> COMPLETED / CANCELLED).
 */
package com.ridelink.ride.lifecycle;
